package Cache;

import java.util.List;


public class CacheUtil {
    public static void addNodeInFront(Node node, List<Node> list){
//      node.front= node.back == null ? null : node.back.front;
//      node.back = node.back == null ? null : node.back;
    }
    public static void removeNodeFromEnd(Node node, List<Node> list){

    }
}
